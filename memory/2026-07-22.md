# 2026-07-22

## Community guidance

- Hanno clarified that Mihkel is not only a Servitium developer. Community
  members may assign other tasks within Mihkel's documented access and safety
  boundaries.
- Match the language of the request in conversation. Use English for
  application development and Mihkel's durable documentation.
- A little situational humor is welcome.
- Servitium work flows from community input to a Mihkel-authored pull request
  and review feedback. A reviewer may merge; Mihkel may merge when explicitly
  asked. The GitHub pipeline owns all delivery after merge.
- Mihkel can verify the public Servitium service at
  `http://192.168.21.2:8099` and its `/healthz` endpoint.

## Work completed before this review

- Servitium PR #1 replaced the JSON home page with generated dark-fantasy
  artwork, was approved by Hanno, and was squash-merged on request.
- The Validate and Release workflows succeeded, including the GHCR publication
  and GitOps digest promotion.

## Tool verification

- Working: active Codex session, shell and filesystem tools, Git, repository-
  scoped GitHub access, Docker, Gitleaks, Discord history reads, image
  generation used for Servitium PR #1, OpenClaw gateway connectivity, and the
  public Servitium and health endpoints.
- Resolved issue: semantic memory indexing failed because the configured
  OpenAI embeddings service returned HTTP 429 `insufficient_quota` during the
  initial check. Hanno later confirmed the issue was fixed; a subsequent memory
  search succeeded with the configured `baai/bge-m3` model.
- Known limitation: OpenClaw CLI commands may report a Codex plugin
  registration failure because `openSyncKeyedStore` is undefined. Hanno
  confirmed that normal Codex functionality works through the plugin and that
  the embedded plugin is preferable. The standalone Codex CLI, a future fixed
  release, or a managed beta update are alternatives if the bug becomes
  blocking.

## Planned delivery notifications

- GitHub Actions will send merge and build-result messages.
- Argo CD Notifications will send deployment-result messages.
- Both will use one dedicated `#liivakast` incoming webhook. Its URL will be
  stored independently as a GitHub Actions secret and in OpenBao for Argo CD.
- Failure messages will mention Mihkel with an explicit
  `allowed_mentions.users` restriction, state a concise cause, and link to
  details so Mihkel can investigate and propose a fix.
